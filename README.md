# Sunday Roast - GitHub Pages site

Clean static-site package for testing on GitHub Pages before reconnecting the custom domain.

Important: this package intentionally contains **no CNAME file**. Do not add one while testing the GitHub Pages URL.

Upload the contents of this folder to the repository root. `index.html` must remain at the repository root.
